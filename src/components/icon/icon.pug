mixin icon(name, modifier)

  - modifier = modifier || ''

  if modifier
    - modifier= `--${modifier}`

  svg(class=`icon icon__${name}${modifier}` role='img')
    use.asd(href=`svg/sprite.svg#${name}`)