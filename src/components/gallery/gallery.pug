mixin gallery()

  - const data = require('./components/gallery/data.json')

  .gallery
    each card in data.cards
      .prod-card 
        img(src=require(`./images/${card.file}`) alt=card.alt)        
        h3= card.h3
        p= card.p